# Core dependencies for MQTT AI Daemon

# MCP (Model Context Protocol) server for AI tool integration
mcp>=1.0.0

# OpenAI SDK for OpenAI-compatible APIs (Ollama, LM Studio, etc.)
openai>=1.0.0

# Testing
pytest>=7.4.0
